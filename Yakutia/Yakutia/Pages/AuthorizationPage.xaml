<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Yakutia.Pages.AuthorizationPage"
             NavigationPage.HasNavigationBar="False">
    <ContentPage.Content>
        <!--Страница авторизации-->
        <StackLayout Margin="20,0" VerticalOptions="Center" Spacing="20">
            <Entry Text="{Binding Email}"  Placeholder="Введите E-mail"/>
            <Entry Text="{Binding Password}"  Placeholder="Введите пароль" IsPassword="True"/>
            <StackLayout>
                <Button Text="Войти" Command="{Binding SignInCommand}"></Button>
                <Button Text="Регистрация" Command="{Binding OpenRegistrationCommand}"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>