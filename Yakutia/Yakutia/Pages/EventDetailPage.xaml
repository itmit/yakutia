<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:forms="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             mc:Ignorable="d"
             x:Class="Yakutia.Pages.EventDetailPage"
             Visual="Material">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Margin="20">
                <Label Text="{Binding Event.Head}" FontFamily="NotoSans-Bold.ttf#NotoSans-Bold" TextColor="Black"/>
                <Label Text="{Binding Event.Place}" FontFamily="NotoSans-Bold.ttf#NotoSans-Bold" TextColor="Black"/>
                <Label Text="{Binding Event.DateStart, StringFormat='{0:dd.MM.yyyy hh:mm:ss}'}" FontFamily="NotoSans-Bold.ttf#NotoSans-Bold" TextColor="Black"/>
                <Button Text="Зарегистрироваться на мероприятие" Command="{Binding RegisterForEventCommand}"/>
                <StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
                    <WebView VerticalOptions="FillAndExpand"
                             MinimumHeightRequest="5"
                             MinimumWidthRequest="5"
                             HorizontalOptions="FillAndExpand"
                             Navigating="WebView_OnNavigating"
                             Source="{Binding HtmlSource}"/>
                </StackLayout>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>