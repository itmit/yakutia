<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Yakutia.Views.PollViewCell">
  <ViewCell.View>
      <StackLayout>
          <StackLayout Margin="20,0">
              <Grid ColumnSpacing="0" Margin="0,20">
                  <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>
                  <Label Grid.Column="0" Text="{Binding PollListNumber, StringFormat='{0}.'}" TextColor="#19365f" FontAttributes="Bold"
                         VerticalOptions="Center"
                         Margin="0,0,5,0"
                         FontSize="16" />
                  <Label Grid.Column="1" Text="{Binding Name}" TextColor="#19365f" FontAttributes="Bold"
                         VerticalOptions="Center" FontSize="16" />
              </Grid>
              <BoxView HeightRequest="1" BackgroundColor="#dee4ea" />
          </StackLayout>
      </StackLayout>
    </ViewCell.View>
</ViewCell>