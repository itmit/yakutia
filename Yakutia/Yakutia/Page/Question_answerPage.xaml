<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Yakutia.Page.Question_answerPage"
             Title="Вопросы—Ответы">
    <ContentPage.Content>
        <!--Страница вопросы-ответы-->
        <ListView HasUnevenRows="True" VerticalScrollBarVisibility="Never" Margin="20,20" ItemsSource="{Binding Briefcase}" SelectedItem="{Binding SelectedResponse}" SeparatorVisibility="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Margin="5,10">
                            <Label Text="{Binding TextResponse}" FontSize="16" TextColor="Black" HorizontalOptions="Start" TextDecorations="Underline"></Label>
                            <Label Text="{Binding TextAnswer}" TextColor="Black" HorizontalOptions="EndAndExpand" IsVisible="{Binding Visible}"></Label>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>